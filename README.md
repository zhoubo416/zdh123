# 工控导航网站

专业的工业自动化行业导航网站，提供英威腾、汇川等知名工控企业官网导航和行业资讯。

## 功能特性

- 🏭 **行业分类导航** - 按变频器、伺服系统、PLC等分类展示企业
- 🔗 **快速导航** - 直接访问企业官网及二级菜单
- 📰 **信息流** - 实时展示行业资讯和企业动态
- 🔍 **智能搜索** - 快速查找企业和产品信息
- 📱 **响应式设计** - 支持桌面端和移动端访问
- ⭐ **收藏功能** - 收藏常用企业和资讯

## 技术栈

- **前端框架**: Nuxt.js 3
- **UI组件**: @nuxt/ui (基于Tailwind CSS)
- **数据库**: PostgreSQL
- **部署**: 支持Vercel、Netlify等平台

## 快速开始

### 环境要求

- Node.js 18+
- PostgreSQL 数据库

### 安装依赖

```bash
npm install
```

### 环境配置

复制 `.env.example` 到 `.env` 并配置数据库连接：

```env
DATABASE_URL=postgresql://username:password@host:port/database
```

### 初始化数据库

```bash
# 启动开发服务器
npm run dev

# 访问 http://localhost:3000/api/init-db (POST请求) 初始化数据库
curl -X POST http://localhost:3000/api/init-db
```

### 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看网站。

## 项目结构

```
industrial-nav-site/
├── components/          # Vue组件
│   ├── CompanyCard.vue  # 企业卡片组件
│   └── NewsFeed.vue     # 信息流组件
├── layouts/             # 布局文件
│   └── default.vue      # 默认布局
├── pages/               # 页面文件
│   └── index.vue        # 首页
├── server/              # 服务端API
│   └── api/             # API路由
├── utils/               # 工具函数
│   ├── database.ts      # 数据库连接
│   └── models.ts        # 数据模型
├── types/               # TypeScript类型定义
├── database/            # 数据库脚本
│   ├── schema.sql       # 数据库架构
│   └── sample-data.sql  # 示例数据
└── assets/              # 静态资源
    └── css/             # 样式文件
```

## API接口

### 企业相关
- `GET /api/companies` - 获取企业列表
- `GET /api/companies?category=1` - 按分类获取企业
- `GET /api/companies/[id]/links` - 获取企业导航链接

### 分类相关
- `GET /api/categories` - 获取所有分类

### 资讯相关
- `GET /api/news` - 获取资讯列表
- `GET /api/news?company=1` - 获取特定企业资讯

### 系统相关
- `POST /api/init-db` - 初始化数据库

## 部署

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

### 部署到Vercel

1. 连接GitHub仓库到Vercel
2. 配置环境变量 `DATABASE_URL`
3. 部署完成后访问 `/api/init-db` 初始化数据库

## 贡献

欢迎提交Issue和Pull Request来改进这个项目。

## 许可证

MIT License